<launch>

  <!-- OMPL Plugin for MoveIt! -->
  <arg name="planning_plugin" value="ompl_interface/OMPLPlanner" />

  <!-- The request adapters (plugins) used when planning with OMPL. 
       ORDER MATTERS -->
  <arg name="planning_adapters" value="default_planner_request_adapters/AddTimeParameterization
				       default_planner_request_adapters/FixWorkspaceBounds
				       default_planner_request_adapters/FixStartStateBounds
				       default_planner_request_adapters/FixStartStateCollision
				       default_planner_request_adapters/FixStartStatePathConstraints" />

  <arg name="start_state_max_bounds_error" value="0.1" />

  <param name="planning_plugin" value="$(arg planning_plugin)" />
  <param name="request_adapters" value="$(arg planning_adapters)" />
  <param name="start_state_max_bounds_error" value="$(arg start_state_max_bounds_error)" />

  <!-- Load OMPL configs. Note: there will be useless parameters if only one arm, or no arm, is present -->
  <rosparam command="load" file="$(find movo_7dof_moveit_config)/config/ompl_planning.yaml" if="$(optenv MOVO_HAS_TWO_7DOF_ARMS false)"/>
  <rosparam command="load" file="$(find movo_7dof_moveit_config)/config/ompl_planning_right_only.yaml" if="$(optenv MOVO_HAS_RIGHT_7DOF_ARM_ONLY false)"/>
</launch>
