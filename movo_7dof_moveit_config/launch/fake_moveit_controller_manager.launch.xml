<launch>

  <!-- Set the param that trajectory_execution_manager needs to find the controller plugin -->
  <param name="moveit_controller_manager" value="moveit_fake_controller_manager/MoveItFakeControllerManager"/>

  <!-- The rest of the params are specific to this plugin -->
  <rosparam file="$(find movo_7dof_moveit_config)/config/fake_controllers.yaml" if="$(optenv MOVO_HAS_TWO_7DOF_ARMS false)"/>
  <rosparam file="$(find movo_7dof_moveit_config)/config/fake_controllers_right_only.yaml" if="$(optenv MOVO_HAS_RIGHT_7DOF_ARM_ONLY false)"/>
  <rosparam file="$(find movo_7dof_moveit_config)/config/fake_controllers_left_only.yaml" if="$(optenv MOVO_HAS_LEFT_7DOF_ARM_ONLY false)"/>
</launch>
